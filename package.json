{
  "name": "@git-manager/monorepo",
  "version": "0.1.3",
  "private": true,
  "description": "Git Manager - Cross-platform desktop application and marketing website for managing local Git repositories",
  "license": "MIT",
  "author": {
    "name": "Vitaliy Dyachuk",
    "url": "https://spiriturban.github.io/"
  },
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "dev": "npx pnpm --filter @git-manager/desktop dev",
    "dev:desktop": "npx pnpm --filter @git-manager/desktop dev",
    "dev:site": "npx pnpm --filter @git-manager/site dev",
    "build": "npx pnpm -r --filter !@git-manager/monorepo build",
    "build:desktop": "npx pnpm --filter @git-manager/desktop build",
    "build:site": "npx pnpm --filter @git-manager/site build",
    "test": "npx pnpm -r --filter !@git-manager/monorepo test",
    "test:ts": "vitest run",
    "test:rust": "cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "typecheck": "npx pnpm -r --filter !@git-manager/monorepo typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npx pnpm typecheck && npx pnpm test:ts && npx pnpm test:rust",
    "release:prepare": "node scripts/sync-version.mjs",
    "version:check": "node scripts/check-version.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "prettier": "^3.5.1",
    "eslint": "^9.20.1"
  }
}
